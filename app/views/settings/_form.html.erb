<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% @setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :companie_id %><br>
    <%= f.number_field :companie_id %>
  </div>
  <div class="field">
    <%= f.label :schedule_interval %><br>
    <%= f.time_select :schedule_interval %>
  </div>
  <div class="field">
    <%= f.label :min_antecedence %><br>
    <%= f.time_select :min_antecedence %>
  </div>
  <div class="field">
    <%= f.label :max_antecedence %><br>
    <%= f.time_select :max_antecedence %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
